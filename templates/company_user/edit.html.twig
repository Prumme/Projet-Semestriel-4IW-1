{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('company_user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_company_user_index', {'company': company.id}) }}">back to list</a>

    {{ include('company_user/_delete_form.html.twig') }}
{% endblock %}
