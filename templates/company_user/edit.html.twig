{% extends 'backoffice.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="flex flex-col gap-4">
        <h1>Edit {{ user.identity }}</h1>
        {{ include('components/buttons/button_link.html.twig',{
            size:'sm',
            bg:'secondary',
            content:"Go back",
            icon_left:"arrow-back-circle-outline",
            link:path('app_company_user_index', {company: company.id})
        }) }}
        {% embed 'components/layouts/form_layout.html.twig' with {'content': 'Update user', icon:"create-outline"} %}
            {% block footer %}
                {% include('components/layouts/delete_form_layout.html.twig') with {
                    item: user,
                    action: path('app_company_user_delete', {company:company.id, id:user.id}),
                } %}
            {% endblock %}
        {% endembed %}
    </div>
{% endblock %}


