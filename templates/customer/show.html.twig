{% extends 'base.html.twig' %}

{% block title %}Customer{% endblock %}

{% block body %}
    <h1>Customer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer.id }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ customer.lastname }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ customer.firstname }}</td>
            </tr>
            <tr>
                <th>Company_name</th>
                <td>{{ customer.companyName }}</td>
            </tr>
            <tr>
                <th>Company_siret</th>
                <td>{{ customer.companySiret }}</td>
            </tr>
            <tr>
                <th>Company_vat_number</th>
                <td>{{ customer.companyVatNumber }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ customer.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_customer_index', {'company':company.id}) }}">back to list</a>

    <a href="{{ path('app_customer_edit', {'id': customer.id, 'company':company.id}) }}">edit</a>

    {{ include('customer/_delete_form.html.twig') }}
{% endblock %}
