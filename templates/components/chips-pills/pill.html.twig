{# HOW TO USE IT #}

{# 
{% include 'components/chips-pills/pill.html.twig' with {
        'label': 'Pill 1', -- OPTIONAL
        'close_icon': true, 
        'type' : 'danger', -- OPTIONAL
        } 
    %}

    All the parameters are optional, if you don't pass them, the default value will be used.
#}

{# /HOW TO USE IT #}

{# WHAT DOES IT DO #}

{# 
    This component is a pill with a label. 
    It can be used to display a tag or a category.
#}

{# /WHAT DOES IT DO #}

{# PATH #}

{# 
    components/chips-pills/pill.html.twig
#}

{# /PATH #}

{% if close_icon is not defined %}
{% set close_icon = false %}
{% endif %}

<div class="bg-{{ type | default('1Œ') }}-500 inline-flex items-center gap-2.5 px-4 py-1.5 rounded-full text-white border-2 border-{{ type | default('primary') }}-500">
    {% if close_icon %}
        {% include 'svg/chips-pills/close.svg.twig' %}
    {% endif %}
    <p>
        {{ label | default('Pill')}}
    </p>
</div>
