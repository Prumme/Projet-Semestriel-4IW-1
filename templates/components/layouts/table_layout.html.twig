<div class="flex flex-col gap-6">
    <h1 class="dark:text-text-darkmode">{{ title }}</h1>
    {% set filters = filters is defined ? filters : null %}
    {% include "components/tables/table.html.twig" with { table:table, filters: filters, onSelect: onSelect | default('') } %}
</div>